version: '3.7'
services:
  mongo:
    image: mongo:3.6
    ports:
    - 127.0.0.1:27017:27017

  web:
    build: .
    command: bin/rails server --port 3000 --binding 0.0.0.0
    ports:
    - "3000:3000"
    links:
    - mongo
    volumes:
    - .:/application

  rocketjob: